# Template for config/secrets/server.enc.env — encrypt with: sops -e -i server.enc.env
DATABASE_URL=
AUTH_INTERNAL_TOKEN_GATEWAY_SECRET=
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
TURNSTILE_SECRET=
NOTIFY_INTERNAL_SECRET=
REACTION_INTERNAL_SECRET=
JOB_RUNNER_INTERNAL_SECRET=
# Optional secret DB URL used by server-side admin CDC diagnostics for reaction.
# Non-secret CDC names and health URLs live in config/deploy.server.yml clear env.
STATUS_REACTION_DATABASE_URL=
