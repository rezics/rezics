import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	title: `${verbatimTerms.rezics.value} — Identity, structure, and history for content`,
	description: `Explore ${verbatimTerms.rezics.value} products, shared capabilities, and its open platform.`,
};

export default content;
