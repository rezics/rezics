import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `How should an ${verbatimTerms.ai.value} agent receive an ${verbatimTerms.api.value} token?`,
	status: `What does ${verbatimTerms.staffTrusted.value} change?`,
};

export default content;
