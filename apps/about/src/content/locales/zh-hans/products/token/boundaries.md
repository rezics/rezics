- 身份验证、权限检查、安全限制和用量限制分别由独立的中间件负责。
- 实际限制依次由策略配置、单令牌覆盖和单操作覆盖合成，并始终受服务器不可更改的上限约束。
- Standard 和 Staff Trusted 使用不同的验证结构。JSON 配置保存在数据库中，每次读取时都会重新验证。
- Staff Trusted 只提高吞吐量，不会增加权限；它必须设置期限和审计理由，失效或过期后会安全退回 Standard。
- 令牌秘密只显示一次，存储时仅保留哈希。自我查询不会返回哈希、秘密引用或任何凭证内容。
