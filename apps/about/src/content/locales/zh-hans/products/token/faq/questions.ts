import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `${verbatimTerms.ai.value} 代理应该如何获取 ${verbatimTerms.api.value} 令牌？`,
	status: `${verbatimTerms.staffTrusted.value} 会改变什么？`,
};

export default content;
