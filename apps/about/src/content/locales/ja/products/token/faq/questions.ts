import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `${verbatimTerms.ai.value} エージェントへ ${verbatimTerms.api.value} トークンを安全に渡すには？`,
	status: `${verbatimTerms.staffTrusted.value} では何が変わりますか？`,
};

export default content;
