import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `${verbatimTerms.ai.value} 代理應該如何取得 ${verbatimTerms.api.value} 權杖？`,
	status: `${verbatimTerms.staffTrusted.value} 會改變什麼？`,
};

export default content;
