import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `${verbatimTerms.ai.value} 에이전트는 ${verbatimTerms.api.value} 토큰을 어떻게 받아야 하나요?`,
	status: `${verbatimTerms.privilegedTokenPolicy.value}는 무엇을 바꾸나요?`,
};

export default content;
