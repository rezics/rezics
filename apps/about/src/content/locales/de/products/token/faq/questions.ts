import { verbatimTerms } from "@rezics/i18n/verbatim-terms";

const content = {
	preview: `Wie sollte ein KI-Agent ein ${verbatimTerms.api.value}-Token erhalten?`,
	status: `Was ändert ${verbatimTerms.staffTrusted.value}?`,
};

export default content;
