/**
 * Shared horizontal frame for the Zone header and Page content.
 *
 * Keeping this invariant in one place prevents Page blocks from drifting away
 * from the header as either surface evolves.
 */
export const ZoneSurfaceContainerClassName = "mx-auto w-full max-w-6xl px-4 sm:px-6";
