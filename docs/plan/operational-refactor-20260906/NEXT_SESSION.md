# Continuation after the native convergence delivery

The maintainer stopped complex scope expansion on 2026-09-08 and requested a
separate gap report, committed implementation and a clean, linear `main`.
Start with the [convergence report](../../report/REZICS-operational-refactor-convergence-gaps-20260908.md).
Its open gates supersede earlier reboot notes and source-owner snapshots.

## Current baseline

The physical global `unit` parent and old migration chain have been replaced.
Install the single native baseline only on a fresh target. The old Git history
is recovery evidence, not a runtime compatibility layer. Do not replay obsolete
migrations or restore the unfinished participation branch. Legacy data conversion
remains a separate offline program.

Implemented owners, APIs, SDKs and Web flows are described in the report and in
`services/main/src/services/catalog`. Real captured MusicBrainz release publication,
source-update regressions, editor/profile/consent APIs, fresh schema replay and
Atlas drift have distinct recorded evidence; none certifies all source declarations
or production capacity. Frontend rendering remains human acceptance.

## Resume one explicit gap

Choose a bounded item from the report before starting new implementation.
The main remaining groups are:

1. Bangumi fixed/elected wiki semantics, classification and reversible effective
   content rating with manual override protection.
2. VNDB remaining dump assembly, aggregate reads and API/dump combined projection.
3. MusicBrainz secondary SQL families, source redirects, alternative/candidate
   updates and separately elected artwork adapters.
4. Complete semantic export, exact source dispositions and pinned evidence.
5. Private merge review/reconciliation, recommendation recovery, personal Entity
   addresses, production load/recovery and human rendered acceptance.

The linked report retains an unapplied Bangumi research patch. It is not ready
to merge and does not count as implemented coverage. Do not resume by blindly
applying it or rebuilding deleted worktree branches.

## Verification discipline

Read `AGENTS.md` and `CONTRIBUTING.md`; preserve any newer user work. Use current
native Auth/Participation authority and exact relation scope in fixtures. Source
qualifier facts are not globally readable. Provision bounded operational lanes
for the fixture's known buckets instead of disabling admission. Use an explicitly
selected disposable database; never infer permission to reset development data.

Use the official OpenAPI/SDK generators. Backend tests run through the repository
Vitest project; deterministic frontend TypeScript checks do not authorize browser
or rendered testing. Run the owning focused checks before repeating larger suites.
