// Context token functionality removed as part of pure-oauth-auth change.
// The access token now carries name and email_verified claims directly.
// 作为 pure-oauth-auth 变更的一部分，context token 功能已被移除。
// access token 现在直接携带 name 和 email_verified 声明。
