import { SectionBoundary } from "@/components/SectionBoundary";

/**
 * Realm wiki sub-page. Layout provides realm header + tabs.
 * Realm 百科子页面。布局层提供 realm 页头 + tab 导航。
 */
export default function RealmWikiPage() {
  return (
    <SectionBoundary>
      <div className="text-muted-foreground py-8 text-center text-sm">
        Wiki coming soon.
      </div>
    </SectionBoundary>
  );
}
