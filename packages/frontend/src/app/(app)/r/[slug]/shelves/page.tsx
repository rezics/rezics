import { SectionBoundary } from "@/components/SectionBoundary";

/**
 * Realm shelves sub-page. Layout provides realm header + tabs.
 * Realm 书架子页面。布局层提供 realm 页头 + tab 导航。
 */
export default function RealmShelvesPage() {
  return (
    <SectionBoundary>
      <div className="text-muted-foreground py-8 text-center text-sm">
        Shelves coming soon.
      </div>
    </SectionBoundary>
  );
}
