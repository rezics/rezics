import { SectionBoundary } from "@/components/SectionBoundary";

/**
 * Realm posts sub-page. Layout provides realm header + tabs.
 * Realm 帖子子页面。布局层提供 realm 页头 + tab 导航。
 */
export default function RealmPostsPage() {
  return (
    <SectionBoundary>
      <div className="text-muted-foreground py-8 text-center text-sm">
        Post feed coming soon.
      </div>
    </SectionBoundary>
  );
}
