import { SectionBoundary } from "@/components/SectionBoundary";

/**
 * Realm rules sub-page. Layout provides realm header + tabs.
 * Realm 规则子页面。布局层提供 realm 页头 + tab 导航。
 */
export default function RealmRulesPage() {
  return (
    <SectionBoundary>
      <div className="text-muted-foreground py-8 text-center text-sm">
        Rules coming soon.
      </div>
    </SectionBoundary>
  );
}
