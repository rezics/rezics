tab name="app" {
    pane command="task" cwd="../../../package/app" start_suspended=false {
        args "dev"
    }
}

tab name="admin" {
    pane command="task" cwd="../../../package/admin" start_suspended=false {
        args "dev"
    }
}
