# Claude Code
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/agents
# Sandbox decoy files (bind-mounted into project to mask home dotfiles; recreate every session).
# Remove the corresponding line if the project legitimately needs one of these committed
# (e.g. real `.gitmodules` for submodules, or a project-scoped `.mcp.json`).
.bash_profile
.bashrc
.gitconfig
.gitmodules
.idea
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc

# Dependencies
node_modules/

# Build output
dist/
out/
*.tgz
*.exe
storybook-static/
**/storybook-static/

# Prisma generated clients
**/prisma/generated/

# Environment variables
.env
.env.*
!.env.example

# Logs
*.log

# Coverage
coverage/
*.lcov

# Caches
.vite/
.tanstack/
.eslintcache
.cache/
*.tsbuildinfo

# IDE / OS
.idea/
.DS_Store
*.suo
*.sw?
.vscode/mcp.json

# VitePress
**/.vitepress/dist/
**/.vitepress/cache/

# Project-specific
.VSCodeCounter/
example-gitignore/
install/
tailwind.config.js
git-staged.txt
