/**
 * Book Read Feature - Public API
 *
 * This is the single entry point for all book read feature exports.
 * External consumers should ONLY import from this file, never from internal paths.
 *
 * @module feature/book/read
 */

// ============================================================================
// Page Components (Route-level entry points)
// ============================================================================

/** Chapter reading page. */
export {BookReadChapterPage} from './page/ChapterPage';
