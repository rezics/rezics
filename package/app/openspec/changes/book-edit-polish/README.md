# book-edit-polish

Fix book-edit UI issues: remove Input borders, restore MUI buttons, remove Card section borders (Apple-style), fix chapter list height, render leaf chapters as cards with metadata
