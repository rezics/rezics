---
import AboutLayout from "../../layouts/AboutLayout.astro";
import { ABOUT_LOCALES, type AboutLocale } from "../../i18n/locales";

const pages = import.meta.glob("../../content/*/index.mdx", { eager: true });

export function getStaticPaths() {
  return ABOUT_LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as AboutLocale;
const Page = pages[
  `../../content/${locale}/index.mdx`
] as typeof import("../../content/en/index.mdx");
const Content = Page.default;
---

<AboutLayout locale={locale} page="home" frontmatter={Page.frontmatter}>
  <Content />
</AboutLayout>
