import { Meta } from "@storybook/addon-docs/blocks";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "../../shadcn/card";

<Meta title="Foundation/Patterns/Card Surfaces" />

# Card Surfaces

Rezics uses shadcn `<Card>` as the canonical card primitive. Card radius is
small and consistent (`rounded-md`) so image content is not overly clipped.
Large radius remains for modal and overlay surfaces.

All in-flow card surfaces keep the page-color read:

- `plain`: no outer surface, for flat feed/media/list items.
- `contained`: same background with a whisper ring, for ordinary panel cards.
- `elevated`: same background with soft `shadow-1`, for media-rich articles and
  recommendations.

export function MockImage({ tone = "warm" }) {
  const bg =
    tone === "cool"
      ? "linear-gradient(135deg, #dbe3f1 0%, #f8f7f2 55%, #7fa6b8 100%)"
      : "linear-gradient(135deg, #eadfca 0%, #f8f7f2 45%, #d47b63 100%)";

  return (
    <div
      style={{
        aspectRatio: "16 / 9",
        background: bg,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        gap: 12,
      }}
    >
      <div
        style={{
          width: 44,
          height: 72,
          borderRadius: 4,
          background: "var(--colors-surface-canvas)",
          boxShadow: "0 4px 12px rgba(0,0,0,0.12)",
        }}
      />
      <div
        style={{
          width: 44,
          height: 72,
          borderRadius: 4,
          background: "var(--colors-brand-fill)",
          boxShadow: "0 4px 12px rgba(0,0,0,0.12)",
          transform: "rotate(7deg)",
        }}
      />
      <div
        style={{
          width: 44,
          height: 72,
          borderRadius: 4,
          background: "var(--colors-surface-subtle)",
          boxShadow: "0 4px 12px rgba(0,0,0,0.12)",
          transform: "rotate(-6deg)",
        }}
      />
    </div>
  );
}

export function SampleCard({
  surface,
  withImage = false,
  title,
  description,
  tone,
}) {
  return (
    <Card surface={surface} className="max-w-xs">
      {withImage && <MockImage tone={tone} />}
      <CardHeader>
        <CardTitle>{title}</CardTitle>
        <CardDescription>{description}</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="m-0 text-sm leading-ui text-text-secondary">
          Same outer radius, same page-color read. The surface prop controls
          only separation, not the card semantics.
        </p>
      </CardContent>
    </Card>
  );
}

<div className="grid gap-6 bg-surface-canvas p-6 text-text-primary md:grid-cols-3">
  <SampleCard
    surface="plain"
    withImage
    title="Plain with image"
    description="Flat media/list item"
  />
  <SampleCard
    surface="contained"
    withImage
    title="Contained with image"
    description="Whisper-ring card"
    tone="cool"
  />
  <SampleCard
    surface="elevated"
    withImage
    title="Elevated with image"
    description="Media-rich recommendation"
  />
  <SampleCard
    surface="plain"
    title="Plain text card"
    description="Content-led feed item"
  />
  <SampleCard
    surface="contained"
    title="Contained text card"
    description="Default panel card"
  />
  <SampleCard
    surface="elevated"
    title="Elevated text card"
    description="Use sparingly without media"
    tone="cool"
  />
</div>

## Avoid

Do not hand-roll a card by combining a different fill, large radius, and custom
shadow. Pick the closest `surface` value instead.

<div className="max-w-xs rounded-2xl bg-surface-base p-4 shadow-md">
  <div className="aspect-[16/9] rounded-xl bg-surface-subtle" />
  <p className="mb-0 mt-3 text-sm font-medium">Hand-rolled heavy card</p>
</div>

```tsx
<Card surface="plain">...</Card>
<Card surface="contained">...</Card>
<Card surface="elevated">...</Card>
```
