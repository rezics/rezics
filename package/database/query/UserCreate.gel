select (
    insert User {
        name := <str>$name,
        email := <str>$email,
        description := <optional str>$description,
    }
) {
    id,
    name,
    email,
    description,
    created_at,
    updated_at
}
