{
  "name": "rezics-book-library",
  "private": true,
  "scripts": {
    "app:dev": "cd package/app && bun run dev",
    "server:dev": "cd package/server && bun run dev"
  },
  "workspaces": [
    "package/*"
  ]
}
