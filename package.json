{
  "name": "rezics-book-library",
  "private": true,
  "scripts": {
    "app:dev": "cd package/app && bun run dev",
    "server:dev": "cd package/server && bun run dev",
    "deploy": "cd package/util && bun run deploy",
    "knip": "knip"
  },
  "workspaces": [
    "package/*"
  ],
  "devDependencies": {
    "babel-plugin-react-compiler": "^1.0.0",
    "vite": "^8.0.0-beta.16",
    "knip": "^5.85.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.4"
  }
}
