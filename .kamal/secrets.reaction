# reaction unit secrets (merged with .kamal/secrets-common).
SECRETS=$(sops --decrypt config/secrets/reaction.enc.env)
REACTION_DATABASE_URL=$(kamal secrets extract REACTION_DATABASE_URL $SECRETS)
REACTION_INTERNAL_SECRET=$(kamal secrets extract REACTION_INTERNAL_SECRET $SECRETS)
